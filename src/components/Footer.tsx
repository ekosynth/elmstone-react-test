import React from "react";
import styles from "./Footer.module.scss";

const Footer = (): JSX.Element => {
    return (
        <footer className={styles.footer}>
            <h1>© Elmstone Systems Ltd.</h1>
        </footer>
    );
};

export default Footer;
